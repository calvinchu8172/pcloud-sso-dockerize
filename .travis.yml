language: ruby
# cache: bundler

addons:
  apt:
    packages:
      - imagemagick 
      - libmagickwand-dev 
      - qt5-default 
      - libqt5webkit5-dev
      - xvfb
  artifacts:
    s3_region: "us-east-1"
    paths:
      - $HOME/test-plans/*.*

rvm:
  - 2.3.3

install:
  - bundle install

script:
  - rename 's/.example//' config/settings/*.example
  - rename 's/.example//' config/*.example
  - RAILS_ENV=test bundle exec rake db:migrate db:seed
  - mkdir test-plans
  # - xvfb-run -a bundle exec cucumber
  - xvfb-run -a bundle exec cucumber features/Oauth/01_get_grant_code.feature -r features --format html --out test-plans/report.html

before_script:
  - mysql -e 'CREATE DATABASE mycloud_test;'
    
services:
  - mysql
  - redis-server

notifications:
  email: false

branches:
  only:
    - feature/20190821-1-travis-ci-test